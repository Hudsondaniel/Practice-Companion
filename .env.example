# Practice Assistant — Supabase (frontend / Vercel)
# Dashboard → Project Settings → API → Publishable key (sb_publishable_…)

VITE_SUPABASE_URL=https://bfwprrceptvuleeonxvi.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=

# Legacy name still works if you use the old anon JWT instead:
# VITE_SUPABASE_ANON_KEY=

# Optional: AI coach
# VITE_OPENAI_API_KEY=

# ─── NEVER add to Vite or Vercel (server/backend only) ───
# sb_secret_… secret key — full database access, bypasses RLS

# ─── CLI / migrations only (never commit) ───
# DATABASE_URL=postgresql://postgres:YOUR_PASSWORD@db.bfwprrceptvuleeonxvi.supabase.co:5432/postgres
